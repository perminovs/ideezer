upstream web {
  ip_hash;
  server web:8000;
}

server {
  location / {
    proxy_pass http://web/;
  }

  location = /favicon.ico { access_log off; log_not_found off; }

  location /static/ {
      root /app/root/;
  }

  listen 8000;

  server_name localhost;
}
